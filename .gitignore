node_modules/
dist/
*.log
Future Updates.txt

# Build output now lives outside the project folder (see package.json's
# build.directories.output) so Cursor's workspace/indexer never touches it -
# this entry is just a safety net in case anything ever writes here again.
release/

# Agent skills, IDE metadata, and temporary folders
.agents/
.claude/
.temp_agentic_awesome_skills/

# Ignore markdown documentation & skill files (keep README.md tracked)
*.md
!README.md
CHANGELOG.md

# Web landing page folder for Vercel deployment
site/
